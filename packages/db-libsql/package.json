{
  "name": "@mono/db-libsql",
  "version": "0.0.0",
  "private": true,
  "description": "Node adapter: libsql implementation of the @mono/core Db contract (MCP server, Electron main, core tests)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "7.0.2",
    "vitest": "^5.0.1"
  }
}
